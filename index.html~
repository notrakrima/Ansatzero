<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ansatzero | AGI from First Principles</title>
    <style>
        :root {
            --bg-color: #050505;
            --text-color: #ededed;
            --accent-color: #ffffff;
            --subtle-color: #666666;
            --input-border: #333333;
        }
        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
            font-family: -apple-system, BlinkMacSystemFont, "SF Pro Display", "Segoe UI", Roboto, Oxygen, Ubuntu, Cantarell, "Open Sans", "Helvetica Neue", sans-serif;
            background-color: var(--bg-color);
            color: var(--text-color);
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .container {
            text-align: center;
            padding: 0 20px;
            max-width: 900px;
            position: relative;
            z-index: 2;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        h1 {
            font-size: 5rem;
            font-weight: 800;
            letter-spacing: -0.04em;
            margin: 0;
            background: linear-gradient(180deg, #FFFFFF 0%, #888888 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            opacity: 0;
            animation: fadeIn 1.2s ease-out forwards;
        }

        p.tagline {
            font-size: 1.5rem;
            font-weight: 300;
            color: var(--subtle-color);
            margin-top: 20px;
            letter-spacing: 0.02em;
            opacity: 0;
            animation: slideUp 1s ease-out 0.5s forwards;
        }

        .footnote {
            font-size: 0.9rem;
            font-weight: 300;
            color: var(--subtle-color);
            margin-top: 80px;
            max-width: 460px;
            line-height: 1.5;
            opacity: 0;
            animation: slideUp 1s ease-out 1.5s forwards;
        }

        .signup-form {
            margin-top: 40px;
            opacity: 0;
            animation: slideUp 1s ease-out 1s forwards;
            display: flex;
            flex-direction: column;
            align-items: flex-start;
            gap: 18px;
        }

        .signup-row {
            display: flex;
            gap: 10px;
        }

        .captcha-wrapper {
            background: #101010;
            border: 1px solid #333333;
            border-radius: 4px;
            padding: 1px 1px;
            box-shadow: 0 4px 16px rgba(0, 0, 0, 0.6);
            display: inline-flex;
            justify-content: flex-start;
            align-items: center;
        }

        .captcha-wrapper > div[data-netlify-recaptcha] {
            transform: scale(0.75);
            transform-origin: top left;
        }

        input[type="email"] {
            background: transparent;
            border: 1px solid var(--input-border);
            color: var(--text-color);
            padding: 12px 20px;
            font-size: 1rem;
            width: 250px;
            border-radius: 4px;
            outline: none;
            transition: border-color 0.3s ease;
        }

        input[type="email"]:focus {
            border-color: var(--accent-color);
        }

        button {
            background: var(--bg-color);
            color: var(--text-color);
            border: 1px solid #444444;
            padding: 12px 24px;
            font-size: 1rem;
            font-weight: 600;
            border-radius: 4px;
            cursor: pointer;
            transition: background 0.2s ease, color 0.2s ease, border-color 0.2s ease;
        }

        button:hover {
            background: #111111;
            border-color: #666666;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: scale(0.95); }
            to { opacity: 1; transform: scale(1); }
        }

        @keyframes slideUp {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .gradient-orb {
            position: absolute;
            width: 800px;
            height: 800px;
            background: radial-gradient(circle, rgba(255,255,255,0.03) 0%, rgba(0,0,0,0) 70%);
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            z-index: 1;
            pointer-events: none;
        }
    </style>
</head>
<body>

    <div class="gradient-orb"></div>

    <div class="container">
        <h1>Ansatzero</h1>
        <p class="tagline">AGI from first principles.</p>

        <form id="joinForm"
              class="signup-form"
              name="ansatzero-subscribe"
              method="POST"
              action="/success.html"
              data-netlify="true"
              data-netlify-recaptcha="true">

          <div class="signup-row">
            <input type="email" id="email" name="email" placeholder="email address" required />
            <button type="submit">Join</button>
          </div>

          <div class="captcha-wrapper">
            <div data-netlify-recaptcha="true"></div>
          </div>
        </form>

        <p class="footnote">
            Ansatzero is a stepwise, first‑principles program for artificial general intelligence, built on the premise that beyond a certain parameter count, AGI emerges from architecture, not from a race for unbounded scale. It integrates current LLMs into a unified control architecture. We are preparing a controlled beta release. Register your email to receive deployment updates.
        </p>

        <p class="privacy-note">
            We will only use your email address for beta updates; no tracking scripts, no analytics, no sharing.
        </p>

    </div>


</body>
</html>
